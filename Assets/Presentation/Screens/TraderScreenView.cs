namespace Reliquary.Presentation
{
    /// <summary>
    /// What essence can buy. The offer itself belongs to the economy — the price, whether it can be afforded
    /// and what it completes are all rules — so this screen holds the tab and nothing else until that lands.
    /// </summary>
    public sealed class TraderScreenView : ScreenView
    {
        public override void Refresh()
        {
        }
    }
}
